# list, is a sequence of mutable values,
#which we took a look at. And mutable, just meaning we can change the elements in the list.

names = ["raj", "rohan", "chenchu", "preeti", "annu"]
print(names)
print(names[0])
print(names[1])

# you can also add the another numbers
names.append("shiva")
print(names)

# you can also apply the sort function directly beacuse python have already build the sort functionallity inside
names.sort()